exclude: ["params.json", "src", "Gemfile", "Gemfile.lock"]
defaults:
  -
     scope:
       path: "docs"
     values:
       layout: nil
  -
     scope:
       path: "test"
     values:
       layout: nil

markdown: kramdown
